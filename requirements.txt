# Backend dependencies -- the one real list.
#
# Lives at the repository root, and must stay self-contained (no
# "-r other/file"), because the host builds from the root and copies
# this file into the image *before* the rest of the code, to cache the
# install step. A reference to server/requirements.txt pointed at a file
# that wasn't there yet, and failed the build.
#
# The build is rooted here rather than in server/ because CLAUDE.md
# keeps the coach prompts in /prompts, outside the server package.
anthropic==1.5.0
openai==3.14.0
python-dotenv==0.21.0
fastapi==0.115.6
uvicorn==0.34.0
zeroconf==0.151.3
