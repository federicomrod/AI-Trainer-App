# The dependency list lives at the repository root -- see the comment
# there for why. This keeps `pip install -r requirements.txt` working
# from inside server/ for local development.
-r ../requirements.txt
